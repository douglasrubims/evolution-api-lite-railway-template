# Evolution API Lite - Environment Variables
# Railway: scans this file and suggests variables. Values use Railway template syntax.
# Local: copy to .env and replace ${{ }} with actual values for docker-compose.

# Server
SERVER_PORT=8080
SERVER_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}

# Required: API key for authentication (use in header: apikey)
AUTHENTICATION_API_KEY=${{secret(64, "abcdef0123456789")}}

# Database
DATABASE_PROVIDER=postgresql
DATABASE_CONNECTION_URI=${{Postgres.DATABASE_URL}}

# Redis Cache
CACHE_REDIS_ENABLED=true
CACHE_REDIS_URI=${{Redis.REDIS_URL}}

# Local docker-compose only (Postgres service)
POSTGRES_DB=evolution
POSTGRES_USER=evolution
POSTGRES_PASSWORD=evolution
